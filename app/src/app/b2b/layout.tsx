import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Surmount — Advisor',
};

export default function B2BLayout({ children }: { children: React.ReactNode }) {
  return children;
}
